syntax = "proto3";

package iprs.temporal_demo;
option java_multiple_files = true;

message Order{

  repeated Item items = 1;
  double total_price =2;
}

message Item {
  string sku_name =1;
}

service DemoService {
  rpc PlaceOrder(Order) returns(Order) {}
}